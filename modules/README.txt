This is where we'll create our modules.. under our individual names

Then call these modules form outside of git folder as otherwise, running terraform init
downloads a big file in git folder which stops the git replication when you do git push.

Abhi

